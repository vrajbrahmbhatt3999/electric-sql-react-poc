function setMaskBit(array, indexFromStart) {
  const byteIndex = Math.floor(indexFromStart / 8);
  const bitIndex = 7 - indexFromStart % 8;
  const mask = 1 << bitIndex;
  array[byteIndex] = array[byteIndex] | mask;
}
function getMaskBit(array, indexFromStart) {
  const byteIndex = Math.floor(indexFromStart / 8);
  const bitIndex = 7 - indexFromStart % 8;
  return array[byteIndex] >>> bitIndex & 1;
}
export {
  getMaskBit,
  setMaskBit
};
//# sourceMappingURL=bitmaskHelpers.js.map