import { EventNotifier } from './event.js';
class MockNotifier extends EventNotifier {
  notifications;
  constructor(dbName, emitter) {
    super(dbName, emitter);
    this.notifications = [];
  }
  _emit(eventName, notification) {
    super._emit(eventName, notification);
    this.notifications.push(notification);
  }
}
export {
  MockNotifier
};
//# sourceMappingURL=mock.js.map