/// <reference types="node" />
import { DbName } from '../util/types.js';
import { Notification, Notifier } from './index.js';
import { EventNotifier } from './event.js';
import EventEmitter from 'events';
export declare class MockNotifier extends EventNotifier implements Notifier {
    notifications: Notification[];
    constructor(dbName: DbName, emitter?: EventEmitter);
    _emit(eventName: string, notification: Notification): void;
}
