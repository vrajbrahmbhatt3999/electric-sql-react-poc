import React, { createContext, useContext } from "react";
let ElectricContext = createContext(void 0);
function makeElectricContext() {
  const ctx = createContext(void 0);
  ElectricContext = ctx;
  const useElectric = () => useContext(ctx);
  const provider = ({ children, db }) => {
    return /* @__PURE__ */ React.createElement(ctx.Provider, { value: db }, children);
  };
  return {
    ElectricContext: ctx,
    useElectric,
    ElectricProvider: provider
  };
}
export {
  ElectricContext,
  makeElectricContext
};
//# sourceMappingURL=provider.js.map