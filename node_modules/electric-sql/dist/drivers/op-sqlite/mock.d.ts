import { BatchQueryResult, QueryResult } from '@op-engineering/op-sqlite';
import { DbName } from '../../util/types.js';
import { Database } from './database.js';
export declare class MockDatabase implements Database {
    dbName: DbName;
    fail?: Error | undefined;
    constructor(dbName: DbName, fail?: Error | undefined);
    executeAsync(): Promise<QueryResult>;
    executeBatchAsync(): Promise<BatchQueryResult>;
    private resolveIfNotFail;
}
