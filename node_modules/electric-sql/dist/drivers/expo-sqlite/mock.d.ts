import { type Query, type SQLiteCallback } from 'expo-sqlite';
import { Database } from './database.js';
export declare class MockDatabase implements Database {
    _name: string;
    constructor(_name: string);
    execRawQuery(_queries: Query[], _readOnly: boolean, callback: SQLiteCallback): void;
    getRowsModified(): number;
}
