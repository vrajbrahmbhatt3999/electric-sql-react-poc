import { Database, QueryResult } from './database.js';
import { DbName, Statement } from '../../util/index.js';
export declare class MockDatabase implements Database {
    name: DbName;
    fail: Error | undefined;
    constructor(dbName: DbName, fail?: Error);
    exec(_statement: Statement): Promise<QueryResult>;
    stop(): Promise<void>;
}
