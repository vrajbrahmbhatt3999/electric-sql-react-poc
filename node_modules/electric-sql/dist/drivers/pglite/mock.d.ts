import { Database } from './database.js';
import type { PGliteOptions, QueryOptions, Results } from '@electric-sql/pglite';
export declare class MockDatabase implements Database {
    dataDir?: string;
    fail: Error | undefined;
    constructor(dataDir?: string, _options?: PGliteOptions);
    query<T>(_query: string, _params?: any[], _options?: QueryOptions): Promise<Results<T>>;
}
