import { Row, SqlValue } from '../../util/types.js';
export interface Results {
    rows: {
        item(i: number): Row;
        length: number;
        raw?(): Row[];
    };
    rowsAffected: number;
    insertId?: number;
}
export interface QueryExecResult {
    columns: string[];
    values: SqlValue[][];
}
export declare const rowsFromResults: (results: Results) => Row[];
export declare const resultToRows: (res: QueryExecResult) => Row[];
export declare const mockResults: (rows: Row[]) => Results;
