import { SerialDatabaseAdapter } from './index.js';
class MockDatabaseAdapter extends SerialDatabaseAdapter {
  defaultNamespace = "main";
  expectRun;
  expectQuery;
  constructor() {
    super();
  }
  mockRun(check) {
    this.expectRun = check;
  }
  mockQuery(check) {
    this.expectQuery = check;
  }
  async _run(stmt) {
    return await this.expectRun?.(stmt) || Promise.resolve({ rowsAffected: 0 });
  }
  async _query(stmt) {
    return await this.expectQuery?.(stmt) || Promise.resolve([]);
  }
}
export {
  MockDatabaseAdapter
};
//# sourceMappingURL=mock.js.map