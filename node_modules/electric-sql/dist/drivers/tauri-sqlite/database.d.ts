import OriginalDatabase from '@tauri-apps/plugin-sql';
export type Database = Pick<OriginalDatabase, 'execute' | 'select'> & {
    name: string;
};
export declare function createDatabase(name: string): Promise<Database>;
