function forEach(f, xs, cont) {
  if (xs.length === 0) {
    cont();
  } else {
    const [x, ...rest] = xs;
    f(x, () => {
      forEach(f, rest, cont);
    });
  }
}
export {
  forEach
};
//# sourceMappingURL=continuationHelpers.js.map