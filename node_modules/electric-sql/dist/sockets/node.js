import { WebSocket } from "ws";
import { GenericWebSocket } from './genericSocket.js';
class WebSocketNode extends GenericWebSocket {
  constructor(protocolVsn) {
    super();
    this.protocolVsn = protocolVsn;
  }
  socket;
  makeSocket(opts) {
    const ws = new WebSocket(opts.url, [this.protocolVsn]);
    ws.binaryType = "nodebuffer";
    return ws;
  }
}
export {
  WebSocketNode
};
//# sourceMappingURL=node.js.map