import { ConnectionOptions } from './index.js';
import { WebSocket } from 'ws';
import { GenericWebSocket } from './genericSocket.js';
export declare class WebSocketNode extends GenericWebSocket<true> {
    private protocolVsn;
    protected socket?: WebSocket;
    constructor(protocolVsn: string);
    makeSocket(opts: ConnectionOptions): WebSocket;
}
